Command:
python -c "import asyncio; from app.services.login_service import LoginService, LoginValidationError; exec('async def main():\n    service = LoginService()\n    try:\n        await service.initiate_password_login(\'\', \'\', \'\')\n    except LoginValidationError as exc:\n        print(type(exc).__name__)\n        print(str(exc))'); asyncio.run(main())"

Output:
LoginValidationError
username and password are required for password login
